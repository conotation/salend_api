doctype html
head
    meta(charset='utf-8')
    script(src='https://ajax.aspnetcdn.com/ajax/jQuery/jquery-1.12.4.min.js')
body
    script.
        window.Leaf.getResult(
        #{success},
        "#{name}",
        "#{imp}",
        #{price},
        '#{msg}'
        )

